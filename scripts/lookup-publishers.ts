#!/usr/bin/env tsx

console.error('`npm run lookup-publishers` is deprecated.');
console.error('Use `npm run enrich-catalog -- --source all` or `npm run enrich-catalog -- --source google-books` instead.');
process.exit(1);
