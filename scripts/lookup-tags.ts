#!/usr/bin/env tsx

console.error('`npm run lookup-tags` is deprecated.');
console.error('Use `npm run enrich-catalog -- --source all` or `npm run enrich-catalog -- --source open-library` instead.');
process.exit(1);
